name: Release Windows Bundles

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build-win7-pyside2:
    name: Win7 (PySide2, Python 3.8)
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python 3.8
        uses: actions/setup-python@v5
        with:
          python-version: "3.8"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-win7.txt
          pip install pyinstaller
      - name: Build (PySide2)
        run: |
          pyinstaller -y pyinstaller/win7-pyside2.spec
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ShijianWin7
          path: dist/ShijianWin7/**

  build-win10-pyside6:
    name: Win10/11 (PySide6, Python 3.12)
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-win10.txt
          pip install pyinstaller
      - name: Build (PySide6)
        run: |
          pyinstaller -y pyinstaller/win10-pyside6.spec
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ShijianWin10
          path: dist/ShijianWin10/**

  release:
    name: Publish GitHub Release
    needs: [build-win7-pyside2, build-win10-pyside6]
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist_all
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist_all/ShijianWin7/**
            dist_all/ShijianWin10/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
